name: frontend 
description: "Aplikasi Pustaka Digital Al-Qur'an QnA"
publish_to: 'none'

environment:
  sdk: '^3.11.0' 

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  google_fonts: ^6.2.1
  http: ^1.2.0
  cached_network_image: ^3.3.1
  # Gunakan versi terbaru yang seragam
  firebase_core: ^2.24.2 
  firebase_auth: ^4.15.3 
  cloud_firestore: ^4.14.0
  google_sign_in: ^6.2.1
  share_plus: ^10.1.4
  flutter_markdown: ^0.7.6
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true